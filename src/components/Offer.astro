---
import { OFFER } from '../lib/site';
---
<section class="offer reveal">
  <div class="card">
    <p class="offer-lead">{OFFER.lead}</p>
    <p class="offer-text">{OFFER.text}</p>
    <a class="btn-glass" href={OFFER.href}>{OFFER.cta}</a>
  </div>
</section>
